#include<stdio.h>
#include<string.h>
int main()
{   
    char name[30]="shekh khwaja";
    int len=strlen(name);// strlen is inbuilt function of string
    printf("length of the string=%d",len);
    return(0);
     
}